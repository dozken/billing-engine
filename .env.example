# ==============================================================================
# ROOT ENVIRONMENT VARIABLES
# ==============================================================================
# This file contains shared defaults for Docker Compose.
# For local development, services can override these with their own .env files.
#
# Setup Instructions:
# 1. For Docker: Copy this to .env in the root directory
#    cp .env.example .env
#
# 2. For local development without Docker:
#    - Copy to service directories and customize as needed
#    - Service-specific .env files will override these defaults
# ==============================================================================

# ------------------------------------------------------------------------------
# PostgreSQL Database (Docker Compose)
# ------------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=billing_db

# ------------------------------------------------------------------------------
# Subscription Service (Docker Compose Variables)
# ------------------------------------------------------------------------------
SUBSCRIPTION_DATABASE_URL=postgresql://postgres:postgres@postgres:5432/subscription_db?schema=public
SUBSCRIPTION_JWT_SECRET=your-secret-key-change-in-production
SUBSCRIPTION_PORT=3000
SUBSCRIPTION_PAYMENT_SERVICE_URL=http://payment-gateway-service:3001
SUBSCRIPTION_BASE_URL=http://subscription-service:3000

# ------------------------------------------------------------------------------
# Payment Gateway Service (Docker Compose Variables)
# ------------------------------------------------------------------------------
PAYMENT_DATABASE_URL=postgresql://postgres:postgres@postgres:5432/payment_db?schema=public
PAYMENT_PORT=3001
PAYMENT_SUBSCRIPTION_SERVICE_URL=http://subscription-service:3000

# ==============================================================================
# LOCAL DEVELOPMENT NOTES
# ==============================================================================
# When running services locally (without Docker), create .env files in each
# service directory with these unprefixed variables:
#
# subscription-service/.env:
#   DATABASE_URL=postgresql://postgres:postgres@localhost:5432/subscription_db?schema=public
#   JWT_SECRET=your-secret-key-change-in-production
#   PORT=3000
#   BASE_URL=http://localhost:3000
#   PAYMENT_SERVICE_URL=http://localhost:3001
#
# payment-gateway-service/.env:
#   DATABASE_URL=postgresql://postgres:postgres@localhost:5432/payment_db?schema=public
#   PORT=3001
#   SUBSCRIPTION_SERVICE_URL=http://localhost:3000
# ==============================================================================
